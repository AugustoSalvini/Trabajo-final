{
  "name": "aguas-server",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "node src/server.js",
    "migrate": "node -r dotenv/config src/db/migrate.js",
    "migrate:status": "node -e \"import('./src/db/migrate.js').then(m => m.getExecutedMigrations().then(console.log))\""
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "morgan": "^1.10.0",
    "pg": "^8.16.3"
  }
}
